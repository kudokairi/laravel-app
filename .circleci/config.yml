varsion: 2.1
jobs:
  build:
    working_directory: laravel
    docker:
      - image: circleci/php:7.3-node-browsers
    steps:
      - checkout
      - run: ls
      - run: sudo composer self-update --1
      - run: composer install -n --prefer-dist
      - run: npm ci
      - run: npm run dev
      - run:
          name: php test
          command: vemdor/bin/phpunit
